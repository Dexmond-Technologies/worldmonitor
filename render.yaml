services:
  - type: web
    name: world-monitor
    env: node
    buildCommand: npm install && npm run build:desktop
    startCommand: npm start
    envVars:
      - key: node_version
        value: 18.20.0
      - key: VITE_VARIANT
        value: full
